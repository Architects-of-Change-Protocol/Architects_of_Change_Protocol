# RC Dry-Run and Security Evidence — 2026-07-15

Point-in-time evidence for the Release Candidate Readiness sprint. All operations below were
non-destructive: no version was applied to the tracked tree, nothing was published, no dependency
was altered.

## Changeset version dry run (temporary git worktree, discarded afterwards)

`changeset version` executed in a throwaway worktree of the branch head (node_modules symlinked,
worktree removed afterwards; the tracked tree's versions are untouched):

| Package | Before | After (dry run) |
| --- | --- | --- |
| `@aoc/protocol` | 0.1.0 | **0.2.0** |
| `@aoc/audit-sdk` | 0.1.0 | 0.1.1 (its `@aoc/protocol` dependency spec updated to `0.2.0`) |
| `@aoc/capability-tokens` | 0.1.0 | 0.1.1 |
| `@aoc/consent-engine` | 0.1.0 | 0.1.1 |
| `@aoc/scoped-access` | 0.1.0 | 0.1.1 |

- All 7 pending changesets were consumed; per-package `CHANGELOG.md` files were generated
  (protocol's leads with the 0.2.0 minor entry for the public-API stabilization, commit `ab2ac6e`).
- `node scripts/check-version-graph.mjs` **passed inside the dry-run worktree** — the post-bump
  version graph is consistent.
- The worktree was removed; `git worktree list` confirms only the primary tree remains.

## Publish dry run

- `npm publish --dry-run --workspace @aoc/protocol` →
  `npm warn publish Skipping workspace @aoc/protocol, marked as private` (exit 0, nothing
  simulated further). **This is the intentional publication block working as designed**, not a
  failure.
- `npm pack --dry-run ./packages/protocol` (the publication-content simulation): filename
  `aoc-protocol-0.1.0.tgz`, package size 35.5 kB, unpacked 177.3 kB, 216 files, integrity
  `sha512-XjjhSlFZf0k9X…tGSUqkdlAQa7Q==` — matching the recorded release manifest.

## npm audit (no `npm audit fix` executed; dependencies unchanged)

`npm audit` at the branch head: **4 vulnerabilities (1 low, 2 moderate, 1 high)** — all in
**transitive development dependencies** (`isDirect: false` for every finding).

| Package | Severity | Advisory | Runtime vs dev | Reaches the Protocol tarball? | Recommended action |
| --- | --- | --- | --- | --- | --- |
| `picomatch` (≤2.3.1) | High | GHSA-3v7f-55p6-f55p, GHSA-c2c7-rcm5-vvqj (glob matching / ReDoS) | Dev (jest/glob toolchain) | **No** | `npm audit fix` in a dedicated dependency-maintenance PR |
| `js-yaml` (≤3.14.2, 4.0.0–4.1.1) | Moderate | GHSA-h67p-54hq-rp68 (quadratic DoS) | Dev (Changesets tooling) | **No** | same |
| `brace-expansion` (<1.1.13) | Moderate | GHSA-f886-m6hf-6m8v (process hang) | Dev (minimatch toolchain) | **No** | same |
| `@babel/core` (≤7.29.0) | Low | GHSA-4x5r-pxfx-6jf8 (arbitrary file read via sourceMappingURL) | Dev (jest transform) | **No** | same |

**Relevance to the distributed artifact:** `@aoc/protocol` has **zero runtime dependencies** (see
the release manifest); the tarball contains only `LICENSE`, `README.md`, and `dist/`. None of the
findings ship to consumers. They affect only contributor/CI tooling and should be remediated via
`npm audit fix` in a follow-up PR — deliberately **not** performed in this sprint, which froze
dependencies.

## Node compatibility matrix

| Environment | Node | npm | Evidence |
| --- | --- | --- | --- |
| Declared minimum | ≥20 | — | `packages/protocol/package.json` `engines` |
| Protocol CI | 20 | bundled | every workflow pins `node-version: 20`; merged PRs #314/#315 ran green on it |
| This sprint's local battery | v22.22.2 | 10.9.7 | full `validate:release`, `protocol:rc:check`, consumer fixtures |
| Enterprise (reference consumer) | v22.22.2 local, 22 in CI | 10.9.7 | Enterprise `PROTOCOL_CONSUMPTION_EVIDENCE.md` + its `publishability.yml` |

Node versions other than 20 and 22 were not tested and are not claimed.
