# @aoc/protocol Public API

This is the governed list of every export path `@aoc/protocol` publishes, generated from the actual
barrel files under `packages/protocol/src/`. It supersedes ad-hoc inspection of source — if a symbol
isn't listed here, treat it as internal even if it happens to be reachable via a deep import (deep
imports are unsupported and actively blocked, see `scripts/assert-invalid-imports.mjs`).

## Governance policy

- Every public symbol must be reachable through one of the barrels below (`packages/protocol/src/*/index.ts`)
  and declared in `packages/protocol/package.json` `exports`. No other import path is supported.
- Deep imports (`@aoc/protocol/dist/...`, `@aoc/protocol/src/...`, `@aoc/protocol/internal/...`) are
  not supported, regardless of whether they happen to resolve in a given bundler.
- Removing or renaming a stable public export requires a Changeset classified `major` and an entry in
  this document's history.
- Additive, backwards-compatible exports (new fields, new subpaths, new deprecated-but-present aliases)
  require a Changeset classified `minor`.
- Fixes that don't change any exported contract shape require a Changeset classified `patch`.
- Experimental subpaths (`adapters`, `runtime-registry`) may change shape without a major bump while
  they remain marked experimental in `docs/versioning-and-stability.md`, but any such change still
  requires a Changeset (minor or patch) documenting the change.
- Internal APIs (anything not in this table) must never be exported accidentally — this is enforced by
  `__tests__/architecture/protocol-purity.test.ts` asserting the barrel `index.ts` files are the only
  public entry points and by the tarball-content check in `scripts/validate-protocol-consumer.mjs`.

## Export table

| Export path | Symbols | Stability | Runtime/type | Since | Notes |
| --- | --- | --- | --- | --- | --- |
| `@aoc/protocol` (root) | Alias of `./contracts` | Stable | Type-only | 0.1.0 (root export added this sprint) | `"."` in `exports` was added as part of the consumer-readiness sprint; no symbol changes |
| `@aoc/protocol/contracts` | `CanonicalId`, `UtcDateTime`, `ResourceRef`, `Delegation`, `Constraint`, `ProofMetadata`, `CapabilityToken`, `CapabilityGrant`, `AgentScope`, `ContextCondition`, `ConsentGrant`, `PolicyDecision`, `ScopedAccessRequest`, `AuditEventEnvelope`, `TrustDomainIdentifier` | Stable | Type-only | 0.1.0 (`AuditEventEnvelope`'s `occurredAt`/`subject`/`correlationId`/`reasonCodes`/`schemaVersion` fields added in the Public API Stabilization Sprint, additive/minor) | Also re-exports `legacy-contracts` types (compatibility-only, deprecated). `ScopedAccessRequest.requestedScope` is and has always been the sole scope field — there is no `scope`/`action` alias. |
| `@aoc/protocol/errors` | `ProtocolError` and related error types (`protocol-error`), plus `legacy-errors` | Stable | Type-only | 0.1.0 | Pure re-export barrel |
| `@aoc/protocol/claims` | RFC-005 claim family: `primitives`, `claim-enums` (`ClaimType`, `EvidenceType`, `AttestationType`, `VerificationStatus`, `StandingStatus`, `AuthorityStatus`, `DecisionStatus`), `references`, `proofs/*`, `registries/*`, `credentials/*`, `vocabulary/*`, `CanonicalClaim`, `CanonicalAttestation`, `CanonicalVerification`, `CanonicalStanding`, `validateCanonicalStanding`, `isValidCanonicalStanding`, `CanonicalStandingValidationResult`, `CanonicalCapability`, `CanonicalAuthority`, `CanonicalDecision` | Stable | Mixed — `claim-enums` and `legacy-claims` constants are runtime values; contract shapes are types | 0.1.0 (`ClaimType.Derivation` added in SM-05; `CanonicalStanding` runtime validators added in SM-06 — both additive/minor) | Also declares deprecated `LegacyClaim`/`Claim` locally and re-exports `legacy-claims`. `ClaimType` gained the additive `Derivation` member for first-class derivation assertions (`@aoc/protocol/manifest`'s `DerivationClaim`); no existing `ClaimType` value was removed, renamed or re-spelled. SM-06 added the first runtime structural validators for `CanonicalStanding` — the type was previously type-only — because a portability bundle carries standing records across an external trust boundary; they validate shape only and never adjudicate a dispute, order timestamps or resolve a `claimRef` |
| `@aoc/protocol/adapters` | `AdapterResult<T>`, `AdapterLookupContext`, `VerificationKeyDescriptor`, `VerificationKeyResolver`, `RevocationStatus`, `RevocationLookup`, `RegistryLookup`, `TrustRegistryProvider`, `CapabilityLookup`, `AttestationLookup`, `CredentialStatusLookup`, `AuditEventSink`, `SecurityEvent`, `SecurityEventSink`, `ProtocolEvent`, `ProtocolEventSink`, `PolicyDecisionRequest`, `PolicyDecisionResult`, `PolicyDecisionProvider`, `GovernanceDecisionProvider`, `ExecutionAuthorizationRequest`, `ExecutionAuthorizationResult`, `ExecutionAuthorizationProvider`, `VerificationProvider`, `ObservabilityEventSink` | Experimental | Type-only (interfaces) | Added post-0.1.0 (see `docs/architecture/protocol-export-surface-report.md`) | Also declares deprecated aliases `AccessVerificationPort`, `PolicyEvaluatorPort`, `TrustCoordinationPort`, `TrustDomainPort` |
| `@aoc/protocol/runtime-registry` | `AdapterRegistry`, `RuntimeAdapterBootstrap`, `RuntimeBootstrapEngine`, `createAdapterToken`, `AdapterTokens`, `AllAdapterTokens`, `RuntimeProfileValidationMode`, `RuntimeBootstrapStatus`, `RegisteredAdapterStatus`, `RuntimeAdapterBootstrapStatus`, `AdapterRegistryEventType`, `AdapterNotRegisteredError`, `AdapterAlreadyRegisteredError`, `RegistryValidationError` | Experimental | Runtime (classes, frozen const objects, error classes) | Added post-0.1.0 | The only subpath with real executable runtime behavior beyond enum constants |
| `@aoc/protocol/identity` | `SovereignAssetId`, `mintSovereignAssetId`, `parseSovereignAssetId`, `isValidSovereignAssetId`, `assertValidSovereignAssetId`, `ContentIdentity`, `ContentDigestAlgorithm`, `computeContentIdentity`, `isValidContentIdentity`, `contentIdentitiesEqual`, `verifyContentIdentity`, `contentIdentityKey` | Stable, expanding | Mixed | Added post-0.1.0 | Independent UUID asset identity and exact-byte SHA-256 identity; neither contains storage semantics |
| `@aoc/protocol/canonical` | `CANONICAL_JSON_PROFILE`, `canonicalizeJSON` | Stable, expanding | Runtime | Added post-0.1.0 | Deterministic `aoc-canonical-json/1` representation; unsupported profiles fail manifest validation |
| `@aoc/protocol/manifest` | `SovereignManifestV1`, `SignedSovereignManifest`, `SovereignAssetRegistry`, manifest/claim/proof types, builders and validators, `computeManifestDigest`, `generateSovereignKeyPair`, `signSovereignManifest`, `verifySovereignManifest`, `resolveSovereignAsset`, `resolveSovereignAssetVersion`, `DerivationClaim`, `DerivationRelationKind`, `DERIVATION_RELATION_KINDS`, `BuildDerivationClaimInput`, `buildDerivationClaim`, `validateDerivationClaim`, `isValidDerivationClaim`, `validateOriginClaim`, `isValidOriginClaim`, `validateAuthorityClaim`, `isValidAuthorityClaim`, `ClaimValidationResult`, `traceSovereignLineage`, `SovereignLineageTrace`, `SovereignLineageNode`, `SovereignLineageEdge`, `SovereignLineageDirection`, `SovereignLineageTraceSchemaVersion`, `TraceSovereignLineageInput`, `TraceSovereignLineageValidationResult`, `SOVEREIGN_LINEAGE_DIRECTIONS`, `SOVEREIGN_LINEAGE_TRACE_SCHEMA_VERSION`, `DEFAULT_SOVEREIGN_LINEAGE_MAX_DEPTH`, `validateTraceSovereignLineageInput`, `isValidTraceSovereignLineageInput`, `isValidSovereignLineageMaxDepth` | Stable, expanding | Mixed | Added post-0.1.0 (derivation claims and lineage traversal added in SM-05, additive/minor) | Versioned canonical manifests, Ed25519 proofs, verification results, a storage-neutral version-preserving registry port, and first-class derivation assertions with pure ancestor/descendant lineage traversal. Lineage is a claim: `SovereignManifestV1` has no `parentId` or derivation field, a `DerivationClaim`'s `subject` is the child and its sources are `SovereignAssetId`s, and nothing — licence, rights, authority, authorship, evidence — is inherited along an edge. `traceSovereignLineage` is pure over caller-supplied claims: no database, index, provider or graph library, deterministic ordering, honest `cycleDetected`/`truncated`. See `docs/architecture/sovereign-asset-core.md` §16 |
| `@aoc/protocol/portability` | `SOVEREIGNTY_PORTABILITY_BUNDLE_SCHEMA_VERSION` (`aoc-sovereignty-portability-bundle/1`), `SovereigntyPortabilityBundleV1`, `SovereigntyPortabilityBundleSchemaVersion`, `PortableSovereignManifestArtifact` (`PortableUnsignedSovereignManifestArtifact` | `PortableSignedSovereignManifestArtifact`), `PortableSovereignClaim`, `PortableSovereignClaimArtifact` (`PortableUnsignedSovereignClaimArtifact` | `PortableSignedSovereignClaimArtifact`), `PORTABLE_SOVEREIGN_MANIFEST_ARTIFACT_KINDS`, `PORTABLE_SOVEREIGN_CLAIM_ARTIFACT_KINDS`, `PortableSovereignManifestArtifactKind`, `PortableSovereignClaimArtifactKind`, `BuildSovereigntyPortabilityBundleV1Input`, `buildSovereigntyPortabilityBundleV1`, `tryBuildSovereigntyPortabilityBundleV1`, `SovereigntyPortabilityBundleBuildResult`, `validateSovereigntyPortabilityBundleV1`, `isValidSovereigntyPortabilityBundleV1`, `SovereigntyPortabilityBundleValidationResult`, `isPortableSovereignManifestArtifact`, `isPortableSovereignClaimArtifact`, `portableManifestOf`, `portableClaimOf`, `serializeSovereigntyPortabilityBundle`, `parseSovereigntyPortabilityBundle`, `SovereigntyPortabilityBundleParseResult`, `SOVEREIGNTY_PORTABILITY_REASON_CODES`, `SovereigntyPortabilityReasonCode` | Stable | Mixed — frozen constant data, pure builders/validators, and the canonical serializer/parser | Added post-0.1.0 (new subpath added in SM-06, additive/minor) | The canonical sovereign portability bundle: a versioned, deterministic, provider-neutral representation of one `SovereignSubjectRef` plus the manifest, claim and standing artifacts a caller supplied for it. Six envelope fields and no more — deliberately no `bundleId`, no `exportedAt`, no bundle digest/hash/signature, no provider/storage/tenant state, no content bytes, no completeness flag and no licence/ownership/governance semantics. Serialization is the existing `aoc-canonical-json/1` and no second canonicalizer, archive format, compression or encryption is introduced. Envelope arrays are canonically ordered (manifests by `manifestVersion`, claims by claim `id`, standings by `id`), caller arrays are never mutated and nested artifacts are never rewritten, re-signed, re-digested or normalized. `parseSovereigntyPortabilityBundle` is an external trust boundary and fails closed — including on a *future* bundle schema or an unknown artifact kind, which are rejected rather than best-effort imported or silently dropped. Structural validity is reported as `valid`, never `verified`: nothing here signs, verifies, hashes, resolves an `evidenceRef` or dereferences a locator. Runtime dependencies are `@aoc/protocol/canonical`, `/identity`, `/manifest` and `/claims`; no import-time side effects. See `docs/protocol/SOVEREIGNTY_CAPABILITIES.md` |
| `@aoc/protocol/interoperability` | `SOVEREIGNTY_INTEROPERABILITY_PROFILE_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-profile/1`), `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_ID`, `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_VERSION`, `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_V1`, `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_REF`, `AOC_SOVEREIGNTY_PORTABILITY_MEDIA_TYPE`, `SovereigntyInteroperabilityProfileV1`, `SovereigntyInteroperabilityProfileSchemaVersion`, `SovereigntyInteroperabilityProfileRef`, `SovereigntyInteroperabilityRepresentationProfile`, `AocSovereigntyInteroperabilityProfileId`, `AocSovereigntyPortabilityMediaType`, `isValidSovereigntyInteroperabilityProfileV1`, `isValidSovereigntyInteroperabilityProfileRef`, `AOC_SOVEREIGNTY_CORE_SEMANTIC_VOCABULARY`, `AOC_SOVEREIGNTY_SEMANTIC_NAMESPACE`, `AOC_SOVEREIGNTY_SEMANTIC_TERMS`, `AOC_SOVEREIGNTY_SEMANTIC_TERM_IDS`, `AOC_SOVEREIGNTY_SEMANTIC_CATEGORIES`, `AOC_SOVEREIGNTY_SEMANTIC_CATEGORY_IDS`, `AOC_SOVEREIGNTY_SEMANTIC_VOCABULARY_ID`, `AocSovereigntySemanticTermId`, `getAocSovereigntySemanticTerm`, `SOVEREIGNTY_INTEROPERABILITY_ARTIFACT_KINDS`, `SovereigntyInteroperabilityArtifactKind`, `INTEROPERABLE_CLAIM_TYPES`, `InteroperableClaimType`, `INTEROPERABLE_STANDING_STATUSES`, `InteroperabilitySemanticRequirement`, `isSovereigntyInteroperabilityArtifactKind`, `isInteroperableClaimType`, `isInteroperableStandingStatus`, `isValidInteroperabilitySemanticRequirement`, `compareInteroperabilitySemanticRequirements`, `interoperabilitySemanticRequirementKey`, `SOVEREIGNTY_INTEROPERABILITY_DESCRIPTOR_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-descriptor/1`), `SovereigntyInteroperabilityDescriptorV1`, `SovereigntyInteroperabilityDescriptorSchemaVersion`, `SovereigntyInteroperabilityPresentFeatures`, `buildSovereigntyInteroperabilityDescriptorV1`, `tryBuildSovereigntyInteroperabilityDescriptorV1`, `SovereigntyInteroperabilityDescriptorBuildResult`, `validateSovereigntyInteroperabilityDescriptorV1`, `isValidSovereigntyInteroperabilityDescriptorV1`, `SovereigntyInteroperabilityDescriptorValidationResult`, `presentInteroperabilityArtifactKinds`, `SOVEREIGNTY_INTEROPERABILITY_SUPPORT_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-support/1`), `SovereigntyInteroperabilityConsumerSupportV1`, `SovereigntyInteroperabilitySupportSchemaVersion`, `SovereigntyInteroperabilityProfileSupport`, `buildSovereigntyInteroperabilityConsumerSupportV1`, `BuildSovereigntyInteroperabilityConsumerSupportV1Input`, `validateSovereigntyInteroperabilityConsumerSupportV1`, `isValidSovereigntyInteroperabilityConsumerSupportV1`, `SovereigntyInteroperabilityConsumerSupportValidationResult`, `SOVEREIGNTY_INTEROPERABILITY_REPORT_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-report/1`), `SovereigntyInteroperabilityCompatibilityReportV1`, `SovereigntyInteroperabilityReportSchemaVersion`, `SOVEREIGNTY_INTEROPERABILITY_COMPATIBILITY_STATUSES`, `SovereigntyInteroperabilityCompatibilityStatus`, `SovereigntyInteroperabilityCoreCompatibility`, `assessSovereigntyInteroperabilityCompatibility`, `SOVEREIGNTY_INTEROPERABILITY_REASON_CODES`, `SovereigntyInteroperabilityReasonCode` | Stable | Mixed — frozen constant profile and vocabulary data, pure builders/validators, and the pure compatibility evaluator | Added post-0.1.0 (new subpath added in SM-07, additive/minor) | The self-describing layer around the canonical sovereign representation. `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_V1` is the static Protocol contract naming what an AOC representation family *means* — profile id and version, wire media type, the SM-06 bundle schema, the `aoc-canonical-json/1` profile, the artifact kinds, the claim semantics and the canonical sovereignty semantic vocabulary — so a receiving system can read every negotiable fact without inspecting AOC source. The profile version is deliberately distinct from the npm package version. `SovereigntyInteroperabilityDescriptorV1` describes one *concrete* bundle: which artifact kinds, manifest versions, claim types, standing statuses and semantic concepts are actually present. It duplicates no bundle payload and carries deliberately no `descriptorId` and no `describedAt`, so the same bundle always describes identically. Semantic requirements are extracted from `CanonicalSemanticRef` as `namespace` + `termRef` — concept identity, never the ref's occurrence id. The semantic vocabulary reuses the existing `CanonicalSemanticVocabulary`, `CanonicalSemanticCategory` and `CanonicalSemanticTerm` contracts rather than introducing a parallel model, and stays behaviour-free: it states meanings and evaluates, resolves, scores and decides nothing. `SovereigntyInteroperabilityConsumerSupportV1` is supplied explicitly by the receiving system — never inferred from a user-agent, package or runtime, and never fetched from a well-known URL, DID document, registry or DNS — and carries no consumer identity. Its validator fails closed on unknown vocabulary and on duplicates rather than silently cleaning a machine-readable contract. `assessSovereigntyInteroperabilityCompatibility` is pure and mutates neither argument, distinguishing *core* requirements (profile, media type, bundle schema, canonicalization profile) whose absence is `incompatible` from *feature* requirements whose absence is `partially-compatible`, with explicit sorted gaps and stable reason codes rather than any score. `partially-compatible` never authorizes data loss: nothing here strips, downgrades or projects a bundle. No signature is verified, no digest computed, no identity minted, no policy decided and no trust inferred; there is no external-standard translation (W3C VC, DID, C2PA, SPDX, JSON-LD), no adapter framework and no profile registry. `AOC_SOVEREIGNTY_PORTABILITY_MEDIA_TYPE` is an AOC Protocol media-type identifier and claims no IANA registration. Runtime dependencies are `@aoc/protocol/canonical`, `/identity`, `/manifest`, `/claims` and `/portability`; no import-time side effects. See `docs/protocol/SOVEREIGNTY_CAPABILITIES.md` |
| `@aoc/protocol/sovereignty-capabilities` | `SOVEREIGNTY_CAPABILITY_NAMESPACE`, `SOVEREIGNTY_CAPABILITY_KEYS`, `SOVEREIGNTY_CAPABILITY_IDS`, `SOVEREIGNTY_CAPABILITIES`, `listSovereigntyCapabilities`, `getSovereigntyCapability`, `getSovereigntyCapabilityByKey`, `isSovereigntyCapabilityId`, `isSovereigntyCapabilityKey`, `isSovereigntyCapabilityVersion`, `SovereigntyCapabilityId`, `SovereigntyCapabilityKey`, `SovereigntyCapabilityNamespace`, `SovereigntyCapabilityVersion`, `SovereigntyCapabilityDefinition`, `SovereigntyCapabilityRef`, `toSovereigntyCapabilityRef`, `getSovereigntyCapabilityRef`, `getSovereigntyCapabilityRefByKey`, `isValidSovereigntyCapabilityRef`, `sovereigntyCapabilityRefsEqual`, `SovereigntyCapabilityInvocationId`, `mintSovereigntyCapabilityInvocationId`, `isValidSovereigntyCapabilityInvocationId`, `SOVEREIGNTY_CAPABILITY_INVOCATION_SCHEMA_VERSION`, `SovereigntyCapabilityInvocation`, `SovereigntyCapabilityInvocationSchemaVersion`, `SovereigntyCapabilityInvocationValidationResult`, `BuildSovereigntyCapabilityInvocationInput`, `buildSovereigntyCapabilityInvocation`, `validateSovereigntyCapabilityInvocation`, `isValidSovereigntyCapabilityInvocation`, `SovereigntyCapabilityImplementation`, `SovereigntyCapabilityExecutionOutcome`, `SovereigntyCapabilitySuccessOutcome`, `SovereigntyCapabilityFailureOutcome`, `isValidSovereigntyCapabilityExecutionOutcome`, `SOVEREIGNTY_CAPABILITY_RESULT_SCHEMA_VERSION`, `SovereigntyCapabilityResult`, `SovereigntyCapabilitySuccessResult`, `SovereigntyCapabilityFailureResult`, `SovereigntyCapabilityResultSchemaVersion`, `resolveSovereigntyCapabilitySubject`, `SOVEREIGNTY_CAPABILITY_INVOCATION_EVIDENCE_SCHEMA_VERSION`, `SOVEREIGNTY_CAPABILITY_INVOCATION_EVENT_TYPE`, `SovereigntyCapabilityInvocationEvidenceV1`, `SovereigntyCapabilityInvocationEvidenceSchemaVersion`, `SovereigntyCapabilityInvocationOutcomeStatus`, `isValidSovereigntyCapabilityInvocationEvidence`, `toSovereigntyCapabilityInvocationAuditEvent`, `invokeSovereigntyCapability`, `InvokeSovereigntyCapabilityOptions`, `SovereigntyCapabilityClock`, `SovereigntyCapabilityInvocationError`, `isSovereigntyCapabilityInvocationError`, `SOVEREIGNTY_CAPABILITY_INVOCATION_ERROR_CODES`, `SovereigntyCapabilityInvocationErrorCode`, `SovereigntyCapabilityInvocationErrorDetails`, `CanonicalEvidenceId` (re-export), `createIdentitySovereigntyCapabilityImplementation`, `IdentitySovereigntyCapabilityImplementation`, `IdentitySovereigntyCapabilityInput`, `IdentitySovereigntyCapabilityOutput`, `IdentitySovereigntyCapabilityInputValidationResult`, `IdentitySovereigntyCapabilityReasonCode`, `CreateIdentitySovereigntyCapabilityImplementationOptions`, `validateIdentitySovereigntyCapabilityInput`, `isValidIdentitySovereigntyCapabilityInput`, `IDENTITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `createIntegritySovereigntyCapabilityImplementation`, `IntegritySovereigntyCapabilityImplementation`, `IntegritySovereigntyCapabilityInput`, `IntegritySovereigntyCapabilityOutput`, `IntegritySovereigntyCapabilityInputValidationResult`, `IntegritySovereigntyCapabilityReasonCode`, `IntegritySovereigntyCapabilityOperation`, `ComputeContentIdentityIntegrityInput`, `ComputeContentIdentityIntegrityOutput`, `VerifyContentIdentityIntegrityInput`, `VerifyContentIdentityIntegrityOutput`, `ComputeManifestDigestIntegrityInput`, `ComputeManifestDigestIntegrityOutput`, `IntegrityContentIdentityCheck`, `validateIntegritySovereigntyCapabilityInput`, `isValidIntegritySovereigntyCapabilityInput`, `INTEGRITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `INTEGRITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createProvenanceSovereigntyCapabilityImplementation`, `ProvenanceSovereigntyCapabilityImplementation`, `ProvenanceSovereigntyCapabilityInput`, `ProvenanceSovereigntyCapabilityOutput`, `ProvenanceSovereigntyCapabilityInputValidationResult`, `ProvenanceSovereigntyCapabilityReasonCode`, `ProvenanceSovereigntyCapabilityOperation`, `CreateProvenanceSovereigntyCapabilityImplementationOptions`, `DeclareOriginProvenanceInput`, `DeclareOriginProvenanceOutput`, `DeclareAuthorshipProvenanceInput`, `DeclareAuthorshipProvenanceOutput`, `RecordDerivationProvenanceInput`, `RecordDerivationProvenanceOutput`, `ContestProvenanceClaimInput`, `ContestProvenanceClaimOutput`, `TraceLineageProvenanceInput`, `TraceLineageProvenanceOutput`, `validateProvenanceSovereigntyCapabilityInput`, `isValidProvenanceSovereigntyCapabilityInput`, `PROVENANCE_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `PROVENANCE_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createPortabilitySovereigntyCapabilityImplementation`, `PortabilitySovereigntyCapabilityImplementation`, `PortabilitySovereigntyCapabilityInput`, `PortabilitySovereigntyCapabilityOutput`, `PortabilitySovereigntyCapabilityInputValidationResult`, `PortabilitySovereigntyCapabilityReasonCode`, `PortabilitySovereigntyCapabilityOperation`, `ExportPortabilityBundleInput`, `ExportPortabilityBundleOutput`, `ImportPortabilityBundleInput`, `ImportPortabilityBundleOutput`, `validatePortabilitySovereigntyCapabilityInput`, `isValidPortabilitySovereigntyCapabilityInput`, `PORTABILITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `PORTABILITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createInteroperabilitySovereigntyCapabilityImplementation`, `InteroperabilitySovereigntyCapabilityImplementation`, `InteroperabilitySovereigntyCapabilityInput`, `InteroperabilitySovereigntyCapabilityOutput`, `InteroperabilitySovereigntyCapabilityInputValidationResult`, `InteroperabilitySovereigntyCapabilityReasonCode`, `InteroperabilitySovereigntyCapabilityOperation`, `DescribeInteroperabilityBundleInput`, `DescribeInteroperabilityBundleOutput`, `AssessInteroperabilityCompatibilityInput`, `AssessInteroperabilityCompatibilityOutput`, `validateInteroperabilitySovereigntyCapabilityInput`, `isValidInteroperabilitySovereigntyCapabilityInput`, `INTEROPERABILITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `INTEROPERABILITY_SOVEREIGNTY_CAPABILITY_OPERATIONS` | Stable | Mixed — frozen constant data, pure lookup functions, and the invocation runtime | Added post-0.1.0 (invocation & evidence spine added in SM-03; production Identity and Integrity capsules added in SM-04; production Provenance capsule added in SM-05; production Portability capsule added in SM-06; production Interoperability capsule added in SM-07 — all additive/minor) | Canonical identity, version and discovery for the eight Sovereignty Capabilities, the common contract for invoking one and producing portable capability-attributed evidence, and the first five production capsules. The inventory stays read-only: there is no registration or mutation API, and no global implementation registry — `invokeSovereigntyCapability` takes its implementation as an explicit argument, and the capsule factories have no import-time side effects. Five of the eight (`AOC.IDENTITY`, `AOC.INTEGRITY`, `AOC.PROVENANCE`, `AOC.PORTABILITY`, `AOC.INTEROPERABILITY`) ship production implementations; the other three (Verifiability, Licensing & Terms, Governance Compatibility) do not. Capability versions are unchanged at `1.0.0` — adding an implementation is not a capability-contract change. Provenance requires an existing subject and creates none, never signs or verifies, never mutates a manifest, and inherits nothing along a derivation edge; its lineage traversal is pure over caller-supplied claims with no database or provider. Portability exports and imports the canonical bundle defined by `@aoc/protocol/portability`: `export-bundle` requires an existing subject and never mints one, `import-bundle` works with **no** invocation subject and returns the existing subject that arrived in the bundle, and an explicitly supplied import subject must match it exactly rather than being reconciled. Import is not persistence, registration, verification or trust — nothing is registered, stored, signed, verified, fetched or dereferenced — and the generic evidence carries neither the bundle nor its serialized form. Interoperability describes a bundle and assesses it against a consumer's declared support: `describe-bundle` also works with **no** invocation subject and returns the bundle's existing subject, and an incompatible or partial compatibility report is an ordinary *successful* execution — a capability failure is reserved for input that cannot be read at all. It reuses the Portability bundle contract and validator but never invokes the Portability capsule, verifies no signature, computes no digest, mints no identity, drops no artifact, translates to no external standard and carries no trust, policy or ownership semantics; its generic evidence carries neither the profile, the descriptor, the support declaration nor the report. Runtime dependencies are `@aoc/protocol/identity`, `@aoc/protocol/manifest`, `@aoc/protocol/claims` primitives (capsules only) and `node:crypto` (`randomUUID`); `AuditEventSink`, `AuditEventEnvelope` and `CanonicalEvidenceId` are type-only imports. See `docs/protocol/SOVEREIGNTY_CAPABILITIES.md` |

## Governance decisions (Public API Stabilization Sprint)

- **`AocIdentityClaims` is intentionally not part of this API.** AOC Enterprise's local ambient
  shim (`types/aoc-protocol/index.d.ts`) invented this symbol; it was never exported by any Protocol
  subpath and has no equivalent in Protocol's git history. Protocol's principal-identity surface is,
  by design (see `docs/audits/principal-reference-source-audit.md`), limited to non-verifying
  *references* (`@aoc/protocol/claims` `CanonicalPrincipalRef`) — verified identity/auth claims
  (e.g. token subject claims) are an implementation-specific concern and belong in the consuming
  product, not in Protocol. Enterprise's migration off this import is tracked as separate,
  Enterprise-side follow-up work.
- **`ScopedAccessRequest`'s shape (`principalId`, `resource`, `requestedScope`, `requestedAt`) is
  unchanged and has been canonical since the type's introduction** — there is no historical
  `scope`/`action` shape to preserve compatibility with.
- **`AuditEventEnvelope` gained five additive, optional fields** (`occurredAt`, `subject`,
  `correlationId`, `reasonCodes`, `schemaVersion`) to make the envelope a viable target for
  consumers migrating from richer, product-specific audit event shapes (e.g. Enterprise's
  `event_id`/`occurred_at`/`subject_id`/`requester_id`/`request_id`/`reason` fields). `actorId`
  represents the acting/requesting principal, not the subject; `subject` (new) represents the
  entity the event is about, reusing `ResourceRef` rather than introducing a new shape.

## Verified consumer coverage

Every subpath in the table above is exercised end-to-end (installed from a real `npm pack` tarball,
compiled, and executed) by `npm run protocol:consumer:check` against the fixtures in
`test-consumers/`. See `docs/versioning-and-stability.md` for the CJS/ESM compatibility evidence.
