#!/usr/bin/env node
import { validateRatificationAuthorityCatalog, validateRatificationReadiness, validateRatificationDecisions, validateRatificationSignatures, validateRatificationRelease, validateRatificationLifecycle, validateRatificationAmendmentLock, validateRatificationViolations, requireRatificationFiles, validateRatificationVersionParity } from './ratification-governance-lib.mjs';
import { runScanner } from './constitutional-governance-lib.mjs';
export function scanRatificationGovernance(root){const violations=[];requireRatificationFiles(root,violations);validateRatificationVersionParity(root,violations);validateRatificationAuthorityCatalog(root,violations);validateRatificationReadiness(root,violations);validateRatificationDecisions(root,violations);validateRatificationSignatures(root,violations);validateRatificationRelease(root,violations);validateRatificationLifecycle(root,violations);validateRatificationAmendmentLock(root,violations);validateRatificationViolations(root,violations);return violations;}
if(process.argv[1]&&import.meta.url===new URL(`file://${process.argv[1]}`).href)runScanner('Ratification governance scanner',scanRatificationGovernance);
