#!/usr/bin/env node
import { federationDelegationRecords, federationViolation, VALID_DELEGATION_STATES, FEDERATION_DELEGATION_FILE } from './federation-governance-lib.mjs';
import { runScanner } from './constitutional-governance-lib.mjs';
export function scanFederationDelegation(root){const violations=[];const records=federationDelegationRecords(root);for(const r of records){const id=r['Delegation ID'];if(!id||!/^FDEL-\d{4}$/.test(id))violations.push(federationViolation(FEDERATION_DELEGATION_FILE,`invalid delegation ID '${id}'`,'FED-V-004'));if(r.Status&&!VALID_DELEGATION_STATES.includes(r.Status))violations.push(federationViolation(FEDERATION_DELEGATION_FILE,`${id} has invalid delegation status '${r.Status}'`,'FED-V-004'));if(!r['Source Constitution'])violations.push(federationViolation(FEDERATION_DELEGATION_FILE,`${id} is missing a source constitution`,'FED-V-004'));if(!r['Target Constitution'])violations.push(federationViolation(FEDERATION_DELEGATION_FILE,`${id} is missing a target constitution`,'FED-V-004'));if(!r['Delegated Authority'])violations.push(federationViolation(FEDERATION_DELEGATION_FILE,`${id} is missing a delegated authority`,'FED-V-004'));}return violations;}
if(process.argv[1]&&import.meta.url===new URL(`file://${process.argv[1]}`).href)runScanner('Federation delegation scanner',scanFederationDelegation);
