#!/usr/bin/env node
import { economicsLifecycleRecords, economicsViolation, ECONOMIC_LIFECYCLE_FILE, VALID_LIFECYCLE_STATES, ECONOMICS_TRANSITIONS } from './economics-governance-lib.mjs';
import { runScanner } from './constitutional-governance-lib.mjs';
export function scanEconomicLifecycle(root){const violations=[];const records=economicsLifecycleRecords(root);for(const r of records){const id=r['Transition ID'];if(!id)violations.push(economicsViolation(ECONOMIC_LIFECYCLE_FILE,`missing transition ID`,'ECO-V-008'));const from=r.From,to=r.To;if(from&&!VALID_LIFECYCLE_STATES.includes(from))violations.push(economicsViolation(ECONOMIC_LIFECYCLE_FILE,`transition '${id}' has invalid From state '${from}'`,'ECO-V-008'));if(to&&!VALID_LIFECYCLE_STATES.includes(to))violations.push(economicsViolation(ECONOMIC_LIFECYCLE_FILE,`transition '${id}' has invalid To state '${to}'`,'ECO-V-008'));if(from&&to&&VALID_LIFECYCLE_STATES.includes(from)&&VALID_LIFECYCLE_STATES.includes(to)&&!ECONOMICS_TRANSITIONS.get(from)?.has(to))violations.push(economicsViolation(ECONOMIC_LIFECYCLE_FILE,`transition '${id}' from '${from}' to '${to}' is not a valid transition`,'ECO-V-008'));}return violations;}
if(process.argv[1]&&import.meta.url===new URL(`file://${process.argv[1]}`).href)runScanner('Economics lifecycle scanner',scanEconomicLifecycle);
