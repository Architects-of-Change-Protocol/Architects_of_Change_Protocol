#!/usr/bin/env node
import { reputationRecords, reputationCorrectionRecords, reputationAmendments, reputationViolation, REPUTATION_CORRECTION_FILE } from './reputation-governance-lib.mjs';
import { runScanner } from './constitutional-governance-lib.mjs';
export function scanReputationCorrections(root){const violations=[],reputations=new Map(reputationRecords(root).map(r=>[r['Reputation ID'],r])),records=reputationCorrectionRecords(root);
for(const r of records){const id=r['Correction ID'];if(!/^RCO-\d{4}$/.test(id))violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`invalid correction ID '${id}'`,'REP-V-009'));if(!reputations.has(r['Reputation ID']))violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`${id} references unknown reputation '${r['Reputation ID']}'`,'REP-V-009'));if(!r['Correction Cause'])violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`${id} is missing Correction Cause`,'REP-V-009'));if(!r['Prior Value'])violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`${id} is missing Prior Value`,'REP-V-009'));if(!r['Corrected Value'])violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`${id} is missing Corrected Value`,'REP-V-009'));if(!r.Evidence)violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`${id} is missing Evidence`,'REP-V-009'));if(!r['Decision Reference'])violations.push(reputationViolation(REPUTATION_CORRECTION_FILE,`${id} is missing Decision Reference`,'REP-V-009'));}
return violations;}
if(process.argv[1]&&import.meta.url===new URL(`file://${process.argv[1]}`).href)runScanner('Reputation corrections scanner',scanReputationCorrections);
