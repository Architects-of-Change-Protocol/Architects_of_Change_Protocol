import { landingContent } from '../../lib/content';
import { renderSectionStub } from '../shared';

export function LandingFooter(): string {
  // TODO(figma): Replace this stub with Figma-generated footer component markup.
  return renderSectionStub(landingContent.sections.footer);
}
