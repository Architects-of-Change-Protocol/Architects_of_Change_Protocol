import { landingContent } from '../../lib/content';
import { renderSectionStub } from '../shared';

export function LandingSolution(): string {
  // TODO(figma): Replace this stub with Figma-generated solution section component markup.
  return renderSectionStub(landingContent.sections.solution);
}
