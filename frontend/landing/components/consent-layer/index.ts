import { landingContent } from '../../lib/content';
import { renderSectionStub } from '../shared';

export function LandingConsentLayer(): string {
  // TODO(figma): Replace this stub with Figma-generated consent-layer component markup.
  return renderSectionStub(landingContent.sections.consentLayer);
}
