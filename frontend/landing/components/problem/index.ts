import { landingContent } from '../../lib/content';
import { renderSectionStub } from '../shared';

export function LandingProblem(): string {
  // TODO(figma): Replace this stub with Figma-generated problem section component markup.
  return renderSectionStub(landingContent.sections.problem);
}
