import { landingContent } from '../../lib/content';
import { renderSectionStub } from '../shared';

export function LandingCta(): string {
  // TODO(figma): Replace this stub with Figma-generated CTA component markup.
  return renderSectionStub(landingContent.sections.cta);
}
