import { landingContent } from '../../lib/content';
import { renderSectionStub } from '../shared';

export function LandingSocialProof(): string {
  // TODO(figma): Replace this stub with Figma-generated social proof component markup.
  return renderSectionStub(landingContent.sections.socialProof);
}
