# SDK Versioning and Compatibility

- SDK follows semver.
- `1.x` must preserve stable API signatures and runtime wire compatibility.
- Experimental APIs remain out of `@aoc/sdk` and may appear only under explicit experimental packages in future.
- Contract compatibility is pinned by `RUNTIME_CONTRACTS_VERSION`.
