# AOC Enterprise

AOC Enterprise owns production runtime behavior that consumes the passive contracts and adapter interfaces published by `@aoc/protocol`. Enterprise may implement persistence, verification, trust coordination, observability, and workflow behavior; it must not redefine Protocol contracts, claims, fixtures, or errors.
