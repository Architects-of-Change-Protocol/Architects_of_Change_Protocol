# Verification

**Owner:** AOC Enterprise Assurance.

Owns verification engines, verifier workflows, key-resolution implementations, and orchestration through `VerificationProvider` and `VerificationKeyResolver`. Protocol-owned contracts, claims, errors, fixtures, and adapter interfaces are consumed but never redefined here.
