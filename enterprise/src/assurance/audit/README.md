# Audit

**Owner:** AOC Enterprise Assurance.

Owns audit event processing, append-only/in-memory storage implementations, signed chain verification, replay-oriented querying, and audit sink adapters. Protocol-owned contracts, claims, errors, fixtures, and adapter interfaces are consumed but never redefined here.
