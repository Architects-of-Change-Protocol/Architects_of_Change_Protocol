# Trust

**Owner:** AOC Enterprise Assurance.

Owns trust-registry storage, lookup, synchronization, provider implementations, and enterprise identity trust workflows through `RegistryLookup` and `TrustRegistryProvider`. Protocol-owned contracts, claims, errors, fixtures, and adapter interfaces are consumed but never redefined here.
